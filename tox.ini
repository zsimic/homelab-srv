[tox]
envlist = py{38}, coverage, docs, style

[testenv]
passenv = CI GITHUB_*
setenv = COVERAGE_FILE={toxworkdir}/.coverage.{envname}
usedevelop = True
deps =
    -rrequirements.txt
    -rtests/requirements.txt
commands = pytest {posargs:-vv --cov=homelab_srv --cov-report=xml --junit-xml="{envdir}/junit.xml"}

[testenv:coverage]
setenv = COVERAGE_FILE={toxworkdir}/.coverage
skip_install = True
deps = coverage
commands = coverage combine
           coverage report -m
           coverage xml
           coverage html

[testenv:black]
skip_install = True
deps = black
commands = black {posargs:-l140 homelab_srv.py tests/ setup.py}

[testenv:docs]
skip_install = True
deps = readme-renderer
commands = python setup.py check --strict --restructuredtext

[testenv:style]
skip_install = True
deps = flake8
       flake8-import-order
commands = flake8 {posargs:homelab_srv.py tests/ setup.py}

[testenv:security]
skip_install = True
deps = bandit
commands = bandit {posargs:-r homelab_srv.py}

[testenv:venv]
envdir = .venv
usedevelop = True
deps =
    -rrequirements.txt
    -rtests/requirements.txt
commands = {posargs:python --version}

[coverage:xml]
output = .tox/test-reports/coverage.xml
[coverage:html]
directory = .tox/test-reports/htmlcov

[flake8]
max-line-length = 140
max-complexity = 24
show-source = True
# See https://github.com/PyCQA/flake8-import-order
import-order-style = edited
application-import-names = homelab_srv
